function removeExclamationMarks(s) {    
    

    //spit method breaks down the array amd remove the element with "!" the join combines the array to a string
    let newWord = s.split("!").join("")
    
    
    return newWord
    
  }           