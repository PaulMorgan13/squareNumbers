function repeatStr (n, s) {
  

    //create empty string 
    let newString = ""; 
    
    // loop the array n times
    for (i = 0; i < n; i++){
      

    // Sting summation
     newString = s + newString 
    }
    
    
    //return the newly created string
    return newString;
  }